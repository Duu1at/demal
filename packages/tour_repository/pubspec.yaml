name: tour_repository
description: "Demal APP  Tour Repository."
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  api_client:
  core:
  json_annotation:
  meta:


dev_dependencies:
  build_runner:
  json_serializable:
  very_good_analysis:
  
