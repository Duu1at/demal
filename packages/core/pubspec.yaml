name: core
description: Core package for Demal.
version: 1.0.0
resolution: workspace
publish_to: none

environment:
  sdk: ^3.10.1

flutter:
  generate: true

dependencies:
  flutter:
    sdk: flutter
  api_client:
  json_annotation:
  meta:
  intl:
  smart_auth:
  pinput:
  equatable:

dev_dependencies:
  build_runner:
  json_serializable:
  very_good_analysis:
  flutter_test:
    sdk: flutter 
