name: core
description: "Demal APP Core."
resolution: workspace
version: 0.0.1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  get_it:
  talker:
  talker_flutter:
  go_router:
  image_picker:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
