name: core
description: "Demal APP Core."
resolution: workspace
version: 0.0.1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  get_it:
  talker:
  talker_flutter:
  go_router:
  image_picker:
  url_launcher:
  dio:
  pretty_dio_logger:
  meta:
  connectivity_plus:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
