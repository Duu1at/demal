name: api_client
description: Api cleint for Demal app
version: 0.1.0+1
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  dio:
  connectivity_plus:
  meta:

dev_dependencies:
  mocktail:
  very_good_analysis: 
