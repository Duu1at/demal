name: api_client
description: Api cleint for Demal app
version: 0.1.0+1
resolution: workspace
publish_to: none

environment:
  sdk: ^3.10.1

dependencies:
  connectivity_plus:
  dio:
  flutter:
    sdk: flutter
  meta:
  core:
  app_ui:
  lottie:

dev_dependencies:
  build_runner:
  mocktail:
  very_good_analysis:
  flutter_test:
    sdk: flutter
