name: example
description: "Demal App UI Example"
publish_to: 'none'
resolution: workspace

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  app_ui:
    path: ../ # <-- Говорит, что пакет app_ui находится на уровень выше
  storage:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true
