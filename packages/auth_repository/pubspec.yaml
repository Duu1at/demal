name: auth_repository
description: "Demal APP Auth Repository."
resolution: workspace
version: 0.0.1

environment:
  sdk: ^3.9.0

dependencies:
  core:
  json_annotation:
  meta:
  storage:

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  json_serializable:
  very_good_analysis:
