name: auth_repository
description: "Demal APP Auth Repository."
resolution: workspace
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  api_client:
  profile_repository:
  storage:
  core:
  supabase_flutter:
  google_sign_in:

  json_annotation:
  meta:
  sign_in_with_apple:
  crypto:

dev_dependencies:
  build_runner:
  json_serializable:
  mocktail:
  very_good_analysis:
  flutter_test:
    sdk: flutter
