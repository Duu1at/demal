name: auth_repository
description: "Demal APP Auth Repository."
resolution: workspace
version: 0.0.1

environment:
  sdk: ^3.9.0

dependencies:
  meta:
  json_annotation:
  storage:
  core:

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis:
  json_serializable: 
  build_runner: 
