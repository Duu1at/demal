name: auth_repository
description: "Demal APP Auth Repository."
resolution: workspace
version: 0.0.1  

environment:
  sdk: ^3.10.1

dependencies:
  api_client:
  json_annotation:
  meta:
  storage:

dev_dependencies:
  build_runner:
  json_serializable:
  mocktail:
  very_good_analysis:
  flutter_test:
    sdk: flutter
