name: client_tour_repository
description: "Demal APP Client Tour Repository."
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.9.2

dependencies:
  meta:
  json_annotation:
  core:


dev_dependencies:
  flutter_lints: 
  build_runner: 
  json_serializable:
